/**
 * Register svelte-excalidraw remotes so the default adapter's RPC calls work.
 * The lib's default adapter calls query/command; they must be registered in this app.
 */
import 'svelte-excalidraw/remote';
